package gr.aueb.cf.cf9.ch15;

/**
 * Represents a line segment shape.
 * A line is one-dimensional, so it does not implement {@link ITwoDimensional}.
 * Extends {@link AbstractShape} to inherit id management.
 */
public class Line extends AbstractShape {

    private double length;
}